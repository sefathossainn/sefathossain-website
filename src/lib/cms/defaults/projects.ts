import type { Project } from "@/lib/cms/types";

/**
 * Projects (the visual "build gallery"). Intentionally EMPTY until the real
 * live URLs / screenshots for the Elementor build and Shopify store are
 * supplied — these are the only showable proof and are never mocked up
 * (Visual & Motion Direction §02). Add them in /admin.
 */
export const defaultProjects: Project[] = [];
